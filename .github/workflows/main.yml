name: Analyse TFDefectGA

on:
  push:
    paths:
      - '**.tf'
  workflow_dispatch: {}

jobs:
  tfdefect-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v3

      - name: Lancer TFDefectGA
        uses: TFDefect/TFDefectGA@v1.0.1
        with:
          model: randomforest
          
